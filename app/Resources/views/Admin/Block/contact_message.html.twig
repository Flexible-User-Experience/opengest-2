{% extends sonata_block.templates.block_base %}

{% block block %}
    {% if is_granted('ROLE_CMS') %}
        <div class="box{% if pendingMessages > 0 %} box-danger{% endif %}">
            <div class="box-header">
                <h3 class="box-title">
                    <i class="fa fa-envelope-o" aria-hidden="true"></i>
                    {% if pendingMessages > 0 %}
                        {% if pendingMessages == 1 %}
                            {{ pendingMessages }} missatge de contacte pendent de llegir
                        {% else %}
                            {{ pendingMessages }} missatges de contacte pendents de llegir
                        {% endif %}
                    {% else %}
                        no tens cap missatge pendent de llegir
                    {% endif %}
                </h3>
            </div>
        </div>
    {% endif %}
{% endblock %}
